spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Sanika25More/online-bookstore-microservices
          search-paths: config
          default-label: main
          clone-on-start: true

server:
  port: 8888
